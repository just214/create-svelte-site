import {expect} from '@oclif/test'

describe('create-svelte-app', () => {
  // Todo- Write tests.
  // test
  //   .stdout()
  //   .do(() => cmd.run(['my-app'])).exit(100)
  //   .it('runs hello', ctx => {
  //     expect(ctx.stdout).to.contain('hello world')
  //   })
  expect(true).to.equal(true)
})
